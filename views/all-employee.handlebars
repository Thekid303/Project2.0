<div class="users">
  {{#each employee}}
  <article class="card">
    <div class="card-header d-flex justify-content-center">
      <h2>
        <a href="/user/{{name}}">{{name}}</a>
      </h2>
    </div>
    <div class="card-body">
      {{email}}
    </div>
  </article>
  {{/each}}
</div>