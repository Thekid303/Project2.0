<div class="tasks">
  
  {{#each tasks}}
  <article class="card">
    <div class="card-header d-flex justify-content-center">
      <h2>{{project.name}}</h2>
    </div>
    <div class="card-body">

      {{description}}

    </div>
  </article>
  {{/each}}
</div>